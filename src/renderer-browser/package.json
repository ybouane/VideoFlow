{
  "name": "@videoflow/renderer-browser",
  "version": "1.0.12",
  "description": "Browser-side video renderer for VideoFlow — renders JSON videos using SVG foreignObject + canvas + MediaBunny",
  "license": "Apache-2.0",
  "type": "module",
  "files": ["dist"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npx tsx bundle-worker.ts && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@videoflow/core": "^1.0.12",
    "mediabunny": "^1.40.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
